

/*
    when an arraylist is tend to be full, internally java creates a double(2x) size arraylist,
    and then stores the previous arraylist elements inside the new one,
    then the older arraylist is deleted from the memory
 */
